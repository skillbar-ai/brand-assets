# CI — calls the shared reusable workflow from skillbar-ai/.github
name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ci:
    uses: skillbar-ai/.github/.github/workflows/ci-reusable.yml@main
    secrets: inherit
